// Encuentra el error en este código

const a = 4;
a = 5;

//Las declaraciones "const" no se pueden reasignar ni volver a declarar.

console.log(a);

// Una posible solución (si realemente no necesitas que sea una constante):

let b = 4;
b = 5;

//Las declaraciones "const" no se pueden reasignar ni volver a declarar.

console.log(b);
